Instructions:

1-There is a word document including a report as an executive summary
2-There is a Jupyter Notebook file where the detailed analysis is shown in a notebook
3-There is 6 .py files, you need all of them in the same folder, but you only need to run the main.py dile for the whole code to run, since it is importing the other files. This is the cleaned version from the Jupyter Notebook, excluding unnecesary code that was used in the detailed analysis part
4-The dataset in saved as a CSV file named train.csv, it is saved in a .rar file
